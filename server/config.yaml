circuit:
  description: A quantum circuit made of qubit rows. Each row contains ordered
    line segments. A segment may contain no gate, a standard gate, or a custom
    2x2 matrix gate.
  qubits:
    - id: q0
      label: "|0>"
      y: 160
      segments:
        - id: s0
          gate: none
